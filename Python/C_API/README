使用Python-C-API
优点：
不需要额外的库
很多低级别的控制
从C + +完全可用
不足：
缺点
可能需要努力的大量在代码的开销
必须编译
维护成本高
不同的Python版本不向前兼容的C-API的变化

需要的头文件Python.h
注意：
由Python.h定义，所有用户可见名称的前缀Py的或_Py之一。名与_Py开始是由Python
实习内部使用，不应该在我们的用户程序中出现。结构成员的名字没有一个保留的前缀
